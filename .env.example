# Operator address. Receives converted Rehype USDC (integrator + buyback).
# Also the 95% beneficiary on the curve list (5% Doppler Airlock).
NEXT_PUBLIC_PLATFORM_TREASURY=0x1111111111111111111111111111111111111111

# Optional Base RPC. Falls back to the public viem default.
# NEXT_PUBLIC_BASE_RPC=https://mainnet.base.org

# Optional. Hosted Doppler indexer is Base Sepolia today.
NEXT_PUBLIC_INDEXER_URL=https://test.indexer.doppler.lol

# Avantis v2 tx-builder (pair catalog + position reads).
NEXT_PUBLIC_AVANTIS_API=https://tx-builder.avantisfi.com
